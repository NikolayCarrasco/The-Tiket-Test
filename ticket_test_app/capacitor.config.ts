import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'ticket_test_app',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
